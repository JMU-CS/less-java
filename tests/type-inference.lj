add (a, b) {
    return a + b
}

eq (a, b) {
    return a == b
}

eqBroken (a, b, c, d) {
    return (a == b) && (c == d) && (b == true) && (b == c)
}

eqWorks (a, b, c, d) {
    return (a == b) && (c == d) && (b == c) && (b == true)
}

compareToInt(c) {
    return c == 10
}

compareIntAndBool {
    a = 10
    b = true

    return a == b
}

add (1, 2)
eq (1, 2)

