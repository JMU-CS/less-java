global a = 10
global b = "foo"
global c = false
global d = [1,2,3]
global e = {1,2,3}
global f = <"a" : "b">
global g = []

main() {
    foo()
    println("done")
}

foo() {
    f = <"a" : "c">
    if (f.contains("a")) {
        println(f.get("a"))
    }

    g.add("a")
}
