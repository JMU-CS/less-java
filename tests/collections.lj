nonEmptyListAdd() {
    list = ["a", "b", "c"]
    list.add("d")

    return list
}


test nonEmptyListAdd().size() == 4
test nonEmptyListAdd().get(0) == "a"
test nonEmptyListAdd().get(nonEmptyListAdd().size() - 1) == "d"

emptyListAdd() {
    list = []
    list.add("a")

    return list
}

test emptyListAdd().size() == 1
test emptyListAdd().get(0) == "a"
test emptyListAdd().get(emptyListAdd().size() - 1) == "a"

nonEmptySetAdd() {
    set = {"a", "a", "b", "c"}
    set.add("d")

    return set
}

test nonEmptySetAdd().size() == 4
test nonEmptySetAdd().contains("a")
test !nonEmptySetAdd().contains("e")

nonEmptyMapPut() {
    map = <"x" : 10>
    map.put("y", 100)

    return map
}

test nonEmptyMapPut().containsKey("x")
test nonEmptyMapPut().containsKey("y")
test nonEmptyMapPut().get("x") == 10
test nonEmptyMapPut().get("y") == 100
