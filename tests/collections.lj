list() {
    list = ["a", "b", "c"]
    list.add("d")

    return list
}


test list().size() == 4
test list().get(0) == "a"
test list().get(list().size() - 1) == "d"

list2() {
    list = []
    list.add("a")

    return list
}

test list2().size() == 1
test list2().get(0) == "a"
test list2().get(list2().size() - 1) == "a"

set() {
    set = {"a", "a", "b", "c"}
    set.add("d")

    return set
}

test set().size() == 4
test set().contains("a")
test !set().contains("e")

map() {
    map = <"x" : 10>
    map.put("y", 100)

    return map
}

test map().containsKey("x")
test map().containsKey("y")
test map().get("x") == 10
test map().get("y") == 100
