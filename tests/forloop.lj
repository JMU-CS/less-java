index() {
    list = []
    for (i: 1->10){
        list.add(i)
    }

    return list
}

iterator() {
    list = []

    for (i: ["a", "b", "c"]) {
        list.add(i)
    }

    return list
}

test index()    == [1,2,3,4,5,6,7,8,9]
test iterator() == ["a", "b", "c"]
