A language to introduce students to programming

How to build:
    If you have gradle installed:
        `gradle build`
    If not:
        `./gradlew build`   #unix
        `gradlew build`     #windows
        
